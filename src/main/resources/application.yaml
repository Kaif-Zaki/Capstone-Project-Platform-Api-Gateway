spring:
  application:
    name: api-gateway


  config:
    #    fail-fast no needd since we have a fallback in the gateway
    import: "configserver:"

  cloud:
    config:
      uri: http://config.platform:9000


